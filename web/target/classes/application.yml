spring:
  liquibase:
    enabled: true
    datasource:
      url: jdbc:mysql://localhost:3306/user_service_db
      username: root
      password: password
      jpa:
        database-platform: org.hibernate.dialect.MySQLDialect
        hibernate:
          ddl-auto: validate
          show-sql: true
#  h2:
#    console:
#      enabled: 'true'
#    datasource:
#      url: jdbc:h2:mem:users_repository;DATABASE_TO_UPPER=false;DB_CLOSE_DELAY=-1;
#      driverClassName: org.h2.Driver
#      ddl-auto: create-drop
#    jpa:
#      database-platform: org.hibernate.dialect.H2Dialect

